# 書法字帖平台上線檢查清單

適用範圍：
- 使用者端：`calligraphy-studio`（書法字帖生成）
- 維運端：`font-admin`（字型與授權健康檢查）

---

## 1) 環境與安裝

- [ ] Node.js 版本符合專案需求（建議 18+）
- [ ] `calligraphy-studio` 已執行 `npm install`
- [ ] `font-admin` 已執行 `npm install`
- [ ] `calligraphy-studio` 可正常啟動：`npm run dev`
- [ ] `font-admin` 可正常啟動：`npm run dev`

---

## 2) 必備檔案與設定

### 2.1 字型檔（PDF 中文嵌入）
- [ ] `calligraphy-studio/public/fonts/NotoSansTC-Regular.ttf` 存在
- [ ] `calligraphy-studio/public/fonts/NotoSerifTC-Regular.otf` 存在
- [ ] 字型授權允許嵌入 PDF（法務已確認）

### 2.2 字型清單（授權追蹤）
- [ ] `calligraphy-studio/public/fonts/font-manifest.json` 可讀
- [ ] 每個字型項目都有 `id / displayName / fileName / licenseTag`
- [ ] 每個字型項目都有 `reviewDate`（授權檢視日）

### 2.3 環境變數
- [ ] `OCR_SPACE_API_KEY`（若需 PDF/圖片 OCR）
- [ ] `FONT_ADMIN_KEY`（保護維運 API）

---

## 3) 核心功能驗收（使用者端）

### 3.1 輸入與解析
- [ ] 可貼上純文字後直接生成預覽
- [ ] 上傳 `.txt` 解析成功
- [ ] 上傳 `.docx` 解析成功（含段落/標點優化）
- [ ] 上傳 `.pdf` 可轉為文字（OCR 路徑正常）
- [ ] 上傳圖片（`.png/.jpg/.webp`）可 OCR 解析
- [ ] 檔案過大時有清楚錯誤訊息

### 3.2 字帖生成與預覽
- [ ] 可切換 `毛筆 / 硬筆`
- [ ] 可切換格線（田字/米字/九宮/橫線）
- [ ] 可切換字型選項
- [ ] 字型缺失時有提示且不致中斷流程
- [ ] 生成預覽後會自動捲動到預覽區
- [ ] 「一鍵帶入示例文本」可用

### 3.3 PDF 下載
- [ ] 按下「下載 PDF」可成功下載檔案
- [ ] PDF 中文字可正常顯示（無方塊字）
- [ ] 指定字型不存在時會自動 fallback
- [ ] fallback 時前端有提示（自動降級）

---

## 4) 維運後台驗收（管理端）

- [ ] 開啟 `font-admin` 首頁可見字型健康資料
- [ ] 未提供正確 `FONT_ADMIN_KEY` 時 API 會拒絕（401）
- [ ] 表格可顯示：存在狀態 / 授權標記 / 授權檢視日 / 最後更新
- [ ] 可匯出 JSON 報告
- [ ] 可匯出 CSV 報告
- [ ] 授權狀態可顯示 `正常 / 即將到期 / 已到期`

---

## 5) Build 與部署前檢查

### 5.1 本機 Build
- [ ] `cd calligraphy-studio && npm run lint && npm run build` 成功
- [ ] `cd font-admin && npm run lint && npm run build` 成功

### 5.2 路由與入口
- [ ] 桃花源首頁卡片可進入 `calligraphy-studio/calligraphy`
- [ ] 首頁「管理員維運後台」小連結可進入 `font-admin`
- [ ] `lovereading` 與書法字帖無直接導覽關聯（兩站分離）

### 5.3 回歸測試
- [ ] 深色模式下字帖頁可正常閱讀
- [ ] 手機版（窄螢幕）按鈕與表單可操作
- [ ] API 錯誤訊息對使用者可理解

---

## 6) 上線後監控建議（可選）

- [ ] 追蹤 OCR 失敗率
- [ ] 追蹤 PDF 生成失敗率與平均耗時
- [ ] 每月檢查 `reviewDate` 與授權文件
- [ ] 建立字型檔備份與版本紀錄
