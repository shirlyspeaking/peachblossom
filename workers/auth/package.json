{
  "name": "peachblossom-auth-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply peachblossom-auth --local",
    "db:migrate:remote": "wrangler d1 migrations apply peachblossom-auth --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.83.0"
  }
}
