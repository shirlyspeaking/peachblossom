# 班級小怪獸點名 Demo

這是一個獨立的本機 Demo，先不併入「桃花源」也不部署。

## 功能

- 輸入學生名單，支援換行、逗號、頓號分隔
- 產生每位學生的小怪獸頭像卡片
- 設定抽取人數並啟動隨機點名動畫
- 按下「停！」後顯示中選者
- 使用瀏覽器語音朗讀結果
- 名單暫存在 `localStorage`

## 啟動

```bash
npm install
npm run dev
```

預設本機網址是 `http://localhost:5174/`。
