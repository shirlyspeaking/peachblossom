# 桃花源 Peachblossom — 專案架構說明

本文件說明本網站的組織架構與目錄約定，**修改目錄或新增 app 時請務必遵守**，避免誤把「全站首頁」「個人頁」當成某個 app 的一部分。

---

## 一、網站定位

- **桃花源**（Peachblossom）是**主網站名稱**，不是單一 app 的名稱。
- 桃花源 = 一個**入口網站**，裡面有多個**獨立的 app**，以及**作者的個人頁**。

---

## 二、架構關係（重要）

| 層級 | 說明 | 所在位置 |
|------|------|----------|
| **桃花源首頁** | 全站入口，列出所有 app、作者等 | 根目錄 `index.html` |
| **個人頁 about** | 關於作者，屬於「全站」的一頁，**不是**某個 app 的子頁 | 根目錄 `about.html` |
| **某個 app** | 例如「花店」唐風花鈿，是桃花源「其中一個」應用 | 各自獨立的子資料夾，如 `huadian/` |

**易錯點：**  
不要把 `index.html`（桃花源首頁）或 `about.html`（個人頁）搬進某個 app 的資料夾裡。它們屬於**整站**，必須留在**根目錄**。

---

## 三、目錄結構約定

```
peachblossom/              ← 桃花源根目錄
├── index.html             ← 桃花源首頁（列出所有 app）
├── about.html             ← 個人頁「關於作者」
├── 專案架構說明.md         ← 本說明文件
└── <app 名稱>/            ← 每個 app 單獨一個資料夾
    ├── <app 主頁>.html     ← 該 app 的入口頁
    └── images/            ← 該 app 專用靜態資源（可選，依 app 需要）
        └── …
```

- **根目錄**：只放「全站共用」的頁面（首頁、關於作者）與本架構說明。
- **每個 app**：各自一個資料夾，名稱自訂（如 `huadian`）。該 app 的 HTML、CSS、JS、圖片等都放在**該 app 資料夾內**，必要時可在其下再設 `images/` 等子資料夾。
- **Next.js 類 app**（如 `zhishang-feihua/`）：原始碼與 `package.json` 與靜態入口 `index.html` 可同資料夾並存；首頁卡片連到 `zhishang-feihua/`；**線上互動版**須另以 Cloudflare Pages 部署（見該目錄 `README.md` 的 `npm run pages:deploy`），主站 `peachspring.cc` 僅提供說明與連結頁。

---

## 四、連結約定

- 從**根目錄頁面**（index、about）連到某個 app：  
  使用 **`<app 資料夾>/<該 app 主頁>.html`**  
  例：`huadian/huadian.html`  
  若入口為資料夾預設頁，亦可：**`<app 資料夾>/`**（例：`zhishang-feihua/` → 讀取該目錄下 `index.html`）。
- 從**某個 app 內**連回桃花源首頁或關於頁：  
  使用 **`../index.html`**、**`../about.html`**（回到上一層根目錄）

---

## 五、本次經驗教訓總結

1. **誤解**：曾以為「花店」= 整個網站，把 `index.html`、`about.html` 都移入 `huadian/`，導致桃花源首頁與個人頁變成花店 app 的「子頁」。
2. **正確理解**：桃花源 = 主站；about = 全站個人頁；花店 = 主站底下的一個 app。只有「花店」相關的檔案（如唐風花鈿頁面、唐代背景圖）應放在 `huadian/`，首頁與關於頁必須留在根目錄。
3. **之後做目錄調整或新增 app 時**：先確認要改的是「全站層級」還是「某個 app 層級」，再決定檔案放在根目錄還是哪一個 app 資料夾。

---

## 六、目前實例：花店 app（huadian）

- **路徑**：`huadian/`
- **入口頁**：`huadian/huadian.html`（唐風花鈿 AR 試妝）
- **該 app 專用圖片**：`huadian/images/`（含唐代背景圖、list.json 等）
- 桃花源首頁與關於頁中，連到「唐風花鈿」的連結應為：`huadian/huadian.html`

### 其他已接入首頁的 app

- **山海經捲軸入口**：`shanhaijing-landing/index.html`（Vite + Vue 3 + Three.js 建置產物；原始碼 `shanhaijing-landing-demo/`）
- **悅讀 EnjoyRead**：`enjoyread/app.html`
- **書法字帖生成平台**：`copybook/index.html`
- **Legendary Card Creator**：`legendary-card-creator/index.html`
- **山海經大富翁**：`shanhaijing-monopoly/index.html`
- **山海經大富翁（Vue 重構版）**：`shanhaijing-monopoly-vue/index.html`
- **芯芽**：`class-monster/index.html`（原始碼 `class-monster-demo/`）
- **桃籤・點名台**：`lottery/index.html`（原始碼 `lottery-demo/`）
- **花無缺**（Vite React 原始碼 `hua-wu-que-demo/`，建置產物輸出至 `hua-wu-que/`）：`hua-wu-que/index.html`
- **帛畫探秘**：`mawangdui-silk/play.html`（馬王堆 T 形帛畫神話尋寶，適合五年級課堂）
- **尋姓・鑄徽**：`xunxing/index.html`（古代姓氏溯源、百家姓、取號與族徽鑄印，課堂單頁）。若姓不在五種常見來源（封地／圖騰／居住地／官職／職業）或族譜未收錄，溯源結果會向 `workers/enjoyread-quiz` 的 `POST /origin` 查 DeepSeek，補一句不超過 50 字的來源說明。
- **桃源契文**：`jiagu-name/index.html`（查古字可輸入任意字，下拉選甲骨文／金文／小篆；本班名牆看全名古字；本班名牆小篆體用說文篆書寫全班姓名；猜字謎每人只藏一個古字，先猜字再猜人；也可自己查名字）

---

## 七、部署與存取控制（Cloudflare）

若要把桃花源放到 **Cloudflare** 並以**郵件驗證**限制存取（整站或部分路徑），請依步驟操作：**[docs/Cloudflare-後端與郵件登入.md](docs/Cloudflare-後端與郵件登入.md)**（含 DNS、Pages、Zero Trust Access、保護範圍與選用 Workers 說明）。

若要在 Cloudflare 上自建**全站 Google 登入 API**（中央 Auth Worker，正式網域 **peachspring.cc**），請見：**[docs/桃花源-Google-登入-Worker-部署.md](docs/桃花源-Google-登入-Worker-部署.md)**。

---

撰寫目的：供後續開發與 AI 助手參考，避免再次誤改桃花源架構。  
若新增 app 或調整全站結構，請同步更新本文件。
